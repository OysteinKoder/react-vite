You should do the following:
- Try to make a counter that increments by 1 when you push a button using a let as a variable
- This will fail
- Try to find out why it does not work.
- Read about useState hook.

Challenge: 
- Make a counter that increments by 1 when you push a button using the useState hook instead of a let variable
- Make a counter that decrement by 1 when you push a button using the useState hook instead of a let variable



