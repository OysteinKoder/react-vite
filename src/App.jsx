import "./App.css";
// Import useState here.

function App() {
  // Declare a new useState variable called count here

  // Make a function called incrementCount here, and use it to,,, you guessed it increment the count variable

  // Make a function called decrementCount here, and use it to,,, you guessed it decrement the count variable
  return (
    <>
      {/* Add a button that calls the incrementCount function when clicked */
      /* Add a button that calls the decrementCount function when clicked */
      /* Display the count variable in a h1 tag */}
    </>
  );
}

export default App;
